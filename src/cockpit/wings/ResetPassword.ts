// DO NOT EDIT THIS FILE DIRECTLY!
// Edits will be overwritten on build.
//
// If you would like to make any changes, please edit the source file (src/wings/struct/reset_password.wings)
// and run the following command:
// plz build //src/wings/...

import { IWingsStruct } from 'wings-ts-util';

// 
export default class ResetPassword implements IWingsStruct {
  [key: string]: any;
  public email: String = '';
  public code: String = '';
  public password: String = '';

  public constructor(obj?: any) {
    if (obj) {
      this.email = obj.email !== undefined && obj.email !== null ? obj.email : '';
      this.code = obj.code !== undefined && obj.code !== null ? obj.code : '';
      this.password = obj.password !== undefined && obj.password !== null ? obj.password : '';
    }
  }

  public toJsonKey(key: string): string {
    switch (key) {
      case 'email': {
        return 'email';
      }
      case 'code': {
        return 'code';
      }
      case 'password': {
        return 'password';
      }
      default: {
        return key;
      }
    }
  }
}

{
  "name": "globetrotte",
  "version": "1.0.0",
  "private": true,
  "author": "BinHong Lee <binhong@binhong.me>",
  "scripts": {
    "build": "pnpm -r run build",
    "format": "pnpm -r run format",
    "install_playwright": "cd src/cockpit && pnpx playwright install && pnpm i",
    "lint": "pnpm -r run lint",
    "postinstall": "touch node_modules/go.mod"
  },
  "dependencies": {
  },
  "@comment devDependencies": [
    "This should really be empty but I'm not sure why it fails without it"
  ],
  "devDependencies": {
    "pug": "3.0.2"
  },
  "license": "AGPL 3.0"
}
